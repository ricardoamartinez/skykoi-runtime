---
summary: "CLI reference for `SKYKOI uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `SKYKOI uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
SKYKOI uninstall
SKYKOI uninstall --all --yes
SKYKOI uninstall --dry-run
```
